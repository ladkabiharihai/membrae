---
name: spinning-brain-project
description: "Pragnosia 'spinning brain' — spin-carrier+attention LM (s6_hybrid.py) + living brain.py controller; current direction is the SPIN-DOMINANT architecture (validated), raising it like a child"
metadata:
  node_type: memory
  type: project
  originSessionId: e0d47ca0-8f02-4954-b3c6-8416a10c4dd6
---

`/home/ashish/Downloads/membrae`, branch `growth-refine-1.4b`. The "Pragnosia" spinning brain. Core files: `s6_hybrid.py` (the LM: `SpinAttentionLM` = attention blocks + a diagonal-complex LRU/S5-style **spin carrier**, a parallel associative scan `h_t=λ⊙h_{t-1}+b_t`), `brain.py` (the living controller — honesty, curiosity, learn, grow, memory, cognition), `grow.py` (function-preserving growth), `train_pragnosia.py` (self-governing trainer). `pragnosia.json` = active config. (The 302K `unified_brain` toy was REMOVED — it was never in the live loop; brain.py is now the whole mind.) Data bins (`window2_train`, `big_valid`, `bpe.json`) are gitignored and load-bearing for `brain.py` replay/calibration. **User priorities:** (1) get the architecture right + scaled, (2) raise it like a child (the cognition loop), (3) honest evaluation. NO HARDCODING is a standing directive.

**THE KEY FINDING (2026-06-23, measured):** the scaled 1.4B (48L, mlp12) **drifted to ATTENTION-dominant** — one fixed ~5.25M carrier = **0.37% of params**, brain-swap causal signal only **0.00086 NLL (0.028% of loss)** at 1.4B (down ~20× from 0.016 at 276M). So as built, the spin carrier is vestigial at scale (an external reviewer was right). BUT the user clarified the **intended** design is **SPIN-DOMINANT** (spin = core token-mixer, attention = small helper). Validated by ablation (same tokens/budget, ~37–46M): `carrier="spin_dominant"` (SpinBlock spin-mixer + a `Block` attention layer every 4th) → **ppl 219** vs attention-only **279** vs per-block-hybrid 228 — **−21% at matched params.** The 1.4B was simply built backwards. `carrier` mode is now wired through config→trainer→growth (`none`/`single`/`per_block`/`spin_dominant`). Caveat: small-scale/short-budget/single-seed — **must replicate at 200M–1B (the H100 run should use `carrier:spin_dominant` + a `none` baseline, on the new CoT corpus).** Spin's speed win is LONG-CONTEXT (O(T) vs attention O(T²)) + INFERENCE (recurrent O(1)/token, no KV cache) — NOT short-ctx training (it's slower there; the scan can't be in-place-optimized, breaks autograd; `torch.compile` fuses it on the H100, not the buggy 4060).

**brain.py living loop (all self-derived, no hardcodes; quality follows the model — both models are undertrained):** honesty by self-consistency (`consistency_min` via two-distribution EER), curiosity `wonder()` (own surprise), `teach()` + generative replay, `search()` (Wikipedia), self-governing growth. **In-weights subconscious memory** (`FastWeightMemory`, s6_hybrid): a hippocampal EPISODIC store (model's own key→value traces, surprise-gated write, ATTENTION recall = selective, decay=forget, consolidate-to-slow-weights = sleep), recall **uncertainty-gated** so it doesn't contaminate confident outputs — functional; verbatim recall is model-quality-limited (NOT RAG). **Cognition** (`_introspect`=metacognition, `_deliberate`=step-by-step scratchpad, `think_aloud`=autonomous monologue) wired into `interact()` (empty→monologue, unsure question→deliberate).

**Infra delivered this session:** `find_lr` (Smith range-test → derived peak lr, `--lr 0`); self-governing growth = probe-confirmed saturation, no hardcoded caps (data-budget/VRAM limited); calibration de-hardcoded (match/novelty via EER, learn-min via curve knee, lr-cut via val-noise); bf16 inference (2× faster, ½ mem); full-1.4B training on the 8GB 4060 (bf16 + grad-checkpoint + PagedAdamW8bit); **fixed the Prefetcher CUDA side-stream race** (was the intermittent illegal-memory-access/device-assert crashes); O(T) long-context generation (carrier carries cross-window). User added (H100): CoT corpus (`prepare_scale.py cot_stream`, `cot_build.sh` — targets the multi-step weakness) + true-resume (sidecar `pragnosia_*_state.json`). Deferred no-hardcode (sensitive, need teach-regression tests): teach `plasticity`/`target` clamps; trainer `warm`/`target_eff`. Honest capability: strong single-step (arithmetic/facts/logic), weak multi-hop (relational/analogy/multi-step) = undertraining-for-size — the CoT corpus is the fix.

**Local state:** `pragnosia_best.pt` = the 1.4B (bf16, attention-dominant, the "wrong build", ppl~17, config in `pragnosia.json.1p4B`); pristine fp32 1.4B is on the H100. `pragnosia_spin.pt` is now the **284M spin-dominant** (d768/20L/mlp9, H100-trained to step 418K, **val ppl 24.5**), synced from the H100 + verified locally (bf16 ppl 25.2 ≈ 24.54). **AT-SCALE VALIDATION (2026-06-24, the decisive result):** carrier-ablation → val ppl 25→**7716 (306×)** vs attention 25→85 (3.4×) = the spin carrier is the **LOAD-BEARING CORE** (the opposite of the 1.4B's vestigial 0.028%). Capability emerged: arithmetic 3/3, knowledge 2/3, ToM 2/2 ("France→Paris", "2+2→4"); multi-step/counterfactual still weak; brain.py chat honesty gate **over-conservative on the question-form** (abstains on some knowables — calibration follow-up). The earlier 27M laptop run self-grew 24M(4L)→27.3M(5L) (growth CONFIRMED in production, ppl~40) — the small-scale proof. **2026-06-30:** the H100 grew the model to a **565M snapshot (d768/24L/mlp17, 18 spin + 6 attn, val ppl ~22)**, synced to the laptop as `pragnosia_spin.pt` (the 284M preserved as `pragnosia_284m.*`, still the paper's anchor). **GENERALIZATION CONFIRMED (reviewer's #1):** a diagonal-REAL carrier (no phase, `real_dominant` mode in s6_hybrid) placed as the core ALSO beats attention — multi-seed: attention 360.9±4.5 / complex-spin 115.7±1.8 (3.1×) / **real-no-phase 102.2±2.1 (3.5×, fewer params)** — so "placement matters" is about PLACEMENT (recurrence as core mixer), NOT the complex phase. **Open-model comparison (eval_compare.py, same harness):** our 284M matches/beats GPT-2(124M) + Cerebras-256M on ARC-Easy/HellaSwag, trails on PIQA/LAMBADA (language fluency). Multi-seed ablation harness = `ablation_sweep.py` (per-arch find_lr, resume-safe; per-block + transformer-11L still filling the table). The `carrier="none"` 284M baseline (pragnosia_baseline.json) prepped but NOT run. The 284M (ppl 24.5, 306× ablation) remains the paper's validated anchor. brain.py identity cache is now keyed by arch (was vocab-only → stale-cache crash on model swap). **torch.compile now works on the 4060** (the asserts were the Prefetcher race, fixed) → ~2× (49K→~100K tok/s, bs=24). **Resume restores the peak lr from the sidecar** (find_lr unreliable on trained weights). H100 `window2_train` = `/mnt/kv_cache/pragnosia_data/window2_train.bin`, **330GB / 176.6B tokens** (CoT-enriched); laptop has a 1GB strided CoT subsample. **LONG-CONTEXT (2026-06-30):** the cross-window carrier-carry was NOT wired for spin_dominant (carrier is intra-block; `forward` only threaded single/per_block) → the 565M's effective context = its 256-tok training window (raw-forward ppl ~3× worse past 256: untrained position embeddings). FIXED: `SpinBlock`/`RealBlock` got `forward_state(x,h0)`; model `forward()` threads per-carrier-block state when generating (off in training, so compile/checkpoint untouched). Mechanism verified (18 states thread + change the output) BUT the 565M does NOT yet benefit (next-window ppl 62.6 w/ carry vs 58.0 w/o) — trained fresh-per-window, never learned to USE a cross-window state. Realizing O(T) long-context needs TRAINING with the carry; paper Limitations (v) caveats this honestly. Implemented **windowed-TBPTT long-context training** (`LONG_CTX_W=W`, `LONG_BPTT=1` for full-BPTT write-side) + a **CONTINUAL mode `LONG_MIX=1`**: curriculum mixed-W (short→long) so the model GROWS while training long WITHOUT forgetting. KEY INSIGHT (user's Q "why forget when it grows"): function-preserving growth does NOT forget (the function is preserved at the grow); forgetting only happens if you then train the grown model on a NARROWER objective (pure W=1) that stops reinforcing the carry — so keep the long objective in the training mix (= replay), exactly the continual-learning fix. In LONG_MIX, growth stays ON (probe at W=1, cheap) and the long objective is always rehearsed → grow + long-context coexist. Multi-length val `[W1 W8 W32]`; accurate `seen_toks` accounting (sidecar). Verified grown spin_dominant still threads carrier state (depth+width). **2026-07-03:** the continual LONG_MIX+growth run on the H100 grew + polished to a **706M** snapshot (d768/24L/mlp22, 18 spin + 6 attn, **val ppl 21.97** bf16), synced as the active `pragnosia_spin.pt` (284M anchor kept as `pragnosia_284m.*`; 565M superseded, still on the H100). **LEARNING-CURVE finding (Fig 4, addresses reviewer's 'transformer just isnt trained yet'):** spin vs transformer at ~37M, identical schedule+batches, to 117M tokens — the transformer PLATEAUS (~222) while spin descends to ~56, so the gap WIDENS 2.1×→3.95× (it does NOT narrow). Caveat surfaced: the multi-seed transformer 361 was schedule-limited (short cosine annealed its lr early; full schedule → ~280 at same tokens) — cite the curve, not the brittle snapshots. Paper reworked for 2 reviewer rounds (quality-vs-causal claims separated, 'placement not phase' → budget-qualified, controller marked preliminary, comparison honest, long-context-hurts surfaced); figure on site too. `make_lc_fig.py`/`make_site_lc.py` regenerate the figures from `learning_curves.json`. Workspace cleaned (removed old_versions/, stale _best/_id caches, tarball — freed ~4GB). **706M RIGOROUSLY TESTED (2026-07-03):** carrier **767× load-bearing** (base 22 → carrier-off 16920, attn-off 3.8× — stronger than 284M's 306×); **ALL faculties WIRED** via `python3 brain.py test` (language ppl 22-25, honesty 3/3 known + 1/3 unknowable/over-confident, continual-learning teach→recall=YES+retains, subconscious episodic memory, cognition metacog/deliberate/monologue). **FIX: `teach()` now uses `bnb.PagedAdamW8bit` for big models** (was OOMing the 706M teach on 8GB — the optimizer state is 5.6GB fp32; mirrors the trainer lowmem path) → continual-learning now runs on the big model on a small GPU. **LONG-CONTEXT REALIZED by the LONG_MIX continual training:** carry HELPS (next-window ppl 21.6 vs 22.3, was 62.6 vs 58.0 at 565M) + longer ctx lowers ppl (W32 21.4 vs W1 22.8) = aggregate/topical long-context works (long gens stay on-theme), but sharp NEEDLE retrieval (~300 tok back) still weak (damped carry = gist not verbatim). Paper Limitation (v) flipped 'not realized' → 'aggregate realized at 706M, retrieval weak' (+abstract+Table7+site). **PROGRESSION 284M→565M→706M (all in paper §6 table + fig + site):** val ppl 24.54→22.0→21.97; carrier ablation 306×→(—)→767×; PIQA 59.5→60.7→61.8; ARC-E 34.7→34.1→35.3; ARC-C 24.0→24.9→23.7(chance); HellaSwag 27.9→29.3→29.4; LAMBADA 19.0→21.7→24.4 (biggest gain); multi-hop 2/10→3/10→4/11; long-ctx carry —→hurts→helps. Everything improves/holds; growth+training compound cleanly. **2026-07-07: 1B REACHED (spin_1022M_multistep, d768/29L/mlp27, 22 spin + 7 attn), the reviewers' milestone.** Pulled + tested (active `pragnosia_spin.pt`; 706M→`pragnosia_706m.pt`): **carrier ablation 1886×** (base 20.14→carrier-off 37999; attn-off 4.6×) — the KEY result: causal dominance GROWS with scale (306×@284M → 1081×@706M-converged → 1886×@1B), NOT a small-scale artifact. Benchmarks tie GPT-2-class: **PIQA 63.3** (=GPT-2), **ARC-E 37.2** (≈GPT-2-medium), HellaSwag 29.6, LAMBADA 25.3; multi-hop 4/11 (multi-step+analogy now non-zero). Long-ctx helps (W32 19.2 < W1 20.3); 8k generation runs at CONSTANT 26 tok/s (O(1)/token, no KV-cache) but greedy loops after ~3k (decoding artifact). Progression table+figure (paper+site) now 4-point. Standard val ppl ~20.1 (1B early-in-training); long-ctx best ~17.8. **SELF/PERSONA/GOALS built (brain.py):** self_model (structured honest self-description) + self_report() answers self-questions reliably (interact routes them) — 'No, not conscious, mechanism not experience'; goals stack + set_goal()/pursue_goals() (idle time is goal-directed); `pragnosia_self.txt` chat-format persona corpus for baking it in (trainer `PRAGNOSIA_SELF=` hook). Other H100 session pushes to same remote (their commits: long-ctx paper update, trainer m() compiled forward ~1.5×) — pull before editing. **MULTI-HOP = SCALE NOT ARCHITECTURE (token-matched control, in paper §6):** our 1B (spin) vs Cerebras-GPT-1.3B (transformer, Chinchilla ~20 tok/param ≈ our 24) — on ARC-Challenge BOTH at chance (24.3 vs 25.7), so a token-matched transformer reasons no better at 1.3B → multi-hop weakness is emergence-at-scale, not recurrence-vs-attention. We lag on general-English fluency (LAMBADA 25.3 vs 47, HellaSwag 29.6 vs 35.9) but confounded (Cerebras 29% bigger + Pile + standard tokenizer vs our custom CoT corpus + BPE). **SELF/GOALS refined (no-hardcode):** self_model now DERIVED from live model (_derive_self: reads param count, spin/attn split, abstain_threshold — re-derived on grow, only name+values declared); brain proposes its OWN goals (propose_goal from gaps it hits or curiosity). **teach() OOM-graceful** (1B backprop doesn't fit 8GB → episodic-only + clear msg; `brain.py learn` no longer crashes; use 706M/284M for laptop weight-learning). Chat: `python3 brain.py chat|learn|"one-shot"`. **MIND FACULTIES built (brain.py, all validated on 1B, 2ac665d):** the buildable pieces of a mind on top of the controller — WORKING MEMORY (wm_push + reason(), ~7-slot scratchpad carried across deliberation), AUTOBIOGRAPHY (log_episode + episodes.json timeline, is_memory_question/memory_report answer 'what did I tell you' sharply, persists), AGENCY (_tool_calc exact arithmetic '23+45'→68 [was WRONG before] + act() perceive→act→observe→learn tool loop calc/recall/search), THEORY OF MIND (note_user + user_model, user's mind vs world-fact), REFLECTION (reflect() unsure-questions→gaps→self-goals at idle). Wired into interact(); self_model auto-lists them (derived). Remaining 'real brain' gaps are NOT buildable on a text substrate: reliable knowing (scale/honesty-calibration — training's job), multi-hop reasoning (scale), chat fluency (SFT), and the fundamental ones (grounding/perception/embodiment, valence, consciousness — out of scope, not claimed). Chat quality still raw (model not SFT'd; ~20% of corpus is chat-structured so improves with training + a light SFT upweighting pragnosia_self.txt=57 examples). Decoding fixes: question-detection for no-'?' questions + search-query cleanup. **EXPERIMENTAL 'toward a mind' faculties built (all validated on 1B, honestly labelled, in README 'Toward a mind'; NOT on the public site to avoid anthropomorphizing):** (1) FUNCTIONAL EMOTION (appraise/feel: appraisal→valence/arousal/mood→modulates behavior, derived from own signals; honest 'mechanism not proven-felt, neither claim nor deny'; inherits confidence miscalibration — can feel good about a wrong answer). (2) GROUNDING (search deep-crawl + crawl() page-fetch + provenance topic→source-URL; 'how do you know?'→cites source). (3) GLOBAL WORKSPACE (broadcast() binds focus+memory+goal+mood+last into one attended state; integration() metric 0..1; ACCESS not experience — conditions for emergence, measured not claimed; consciousness must EMERGE not be built, per user). (4) EMBODIMENT (world.py GridWorld + experience(): perceive→act→observe→reward→learn; affect now grounded in consequences — failing nav → genuinely 'frustrated'; nav weak, 1B untrained for it). (5) MULTIMODAL (perception.py PerceptionAdapter + s6_hybrid forward_embeds hook: image→feat→perception-tokens→prepend→FROZEN LM; LLaVA-style, adapter UNTRAINED = 'socket not eye', training it = later GPU job). Commits a88880f→632c02c. Remaining walls: reliable-knowing/reasoning/chat = training's job; the felt aspect of emotion/consciousness = unfalsifiable, not built. **DE-HARDCODED ROUTING (d90f6e7, user caught it):** the meta-question routers (_SELF_Q + is_memory/provenance/workspace_question + memory_report keywords) were hardcoded keyword scans — against the whole no-hardcode principle. Replaced by ONE derived semantic router: `_INTENTS` = seed phrasings per intent (data, like identity_sentences); `_route_intent` picks nearest by the model's OWN embedding similarity, gated by the already-calibrated `match_threshold`, with an `_other` anchor (normal-question prototypes) so the model's similarity separates meta-from-normal (no hand-tuned cutoff). interact/self_report/memory_report dispatch on the routed intent. Validated 11/11 on 1B incl. paraphrases the old lists missed. Only grammatical form-reading remains ('?'/interrogative-opener = sentence TYPE, I/O like intonation, and openers now DERIVED from the corpus via _derive_question_words) + definitional affect signs.

**RESEARCH AGENDA to fix the critic-flagged issues (tasks #25-46, tracker=PENDING_VALIDATION.md; user chose 'build unvalidated, batch-test' since GPU is busy with their Unreal Editor 'Membrae_sam' -- DO NOT kill Unreal, killed it once by mistake).** All new code is ADDITIVE/DORMANT (new methods nothing calls in the hot path) so it can't break the working system. BUILT (13 tasks, committed, PENDING 1B batch-test): T1.1 `_semantic_entropy` (cluster answer-samples by MEANING, entropy over clusters -- the root honesty fix), T1.2 `_rephrasing_stability`, T1.4 `verify_against_source` (grounding as calibration), T1.5 `eval_all.py` (canonical metrics->eval_registry/ JSON, kills scattered numbers), T1.6 `faculty_ablate.py` (each faculty ON/OFF delta), T1.7 graded multi-hop in eval_all, T2.1 `workspace_vector`/`generate_with_workspace` (inject workspace into forward pass; prototype, needs prefix-tuning), T2.2 `goal_vector`, T2.5 `latent_think` (Coconut-style continuous pondering in the carrier -- STANDOUT, native to recurrence), T2.6 `background_tick` (continuous monologue), T3.3 `_mine_corpus_questions` (auto-mined _other anchors -> router robust per-checkpoint), T4.2 relabel no-hardcode claim (DONE, docs), T5.1 `prepare_sft.py` (chat SFT data+recipe, the 'Hi->WW2' fix). REMAINING (9, need GPU-dev or H100-training): T1.3 activation truthfulness probe, T1.8 intermediate-rep probe, T2.3 in-weights-memory-primary, T2.4 self-vector(train), T3.1 matched-fraction ablation, T3.2 activation patching, T4.1 derive mind constants, T5.2 LoRA continual, T5.3 learned growth. First batch-test when GPU free: `/tmp/sement.py` (semantic entropy separation) then `eval_all.py`/`faculty_ablate.py`. **ALL 22 AGENDA TASKS COMPLETE (validated on 1B where possible).** Key findings: honesty gate WIRED (T1.3 activation probe: nonsense->'I'm not sure', known->answer; semantic-entropy T1.1 FAILED [consistent confabulation], grounding T1.4 WORKS, activation-probe T1.3 WORKS -- gate fixes WHEN it speaks, generation quality [Hi->WW2] is the SFT job T5.1, data ready=data/sft.bin). T3.1 matched-fraction ablation (ablate_matched.py): 7-spin catastrophic vs 7-attn 4.8x AT MATCHED COUNT -> layer-count confound REFUTED, carrier claim STRENGTHENS. T1.8 (mech_probe.py): model does NOT compute 2-hop intermediates (France rank 175 but answer rank 1 = memorized shortcut) -> multi-hop is a COMPUTE-step failure, partially challenges 'just undertrained'. T3.2 patching: attn 0.886 > spin 0.745 causal for factual recall (carrier load-bearing globally, recall leans attention). T5.2 (lora.py): teach OOM FIXED (LoRA 11.6M params=1.1%, peak 2.64GB on 1B). T4.1 affect constants derived from surprise autocorrelation. T2.1/2.3/2.4 workspace-inject/in-weights-recall/self-vector (prototypes, T2.1+T2.4 need prefix/self-token training). Scripts: eval_all.py, faculty_ablate.py, ablate_matched.py, mech_probe.py, lora.py, prepare_sft.py. GPU: Unreal Editor 'Membrae_sam' uses it intermittently -- NEVER kill it.

**FLUENCY FIXED (the #1 not-working thing).** KEY: the model is ALREADY fluent in chat format (the user's self-corpus training took) -- 'Hi->WW2 text' was an interact() ROUTING BUG, not a model problem: think()'s reply fed the RAW input to generate_text instead of '<user>..<assistant>'. Fixed -> 'Hi'->greeting, 'Tell me about dogs'->about dogs. Also fixed router misroutes (added greetings + topical 'tell me about X' to _INTENTS _other). On-device LoRA SFT (lora_sft.py, 500 steps, 4.3GB via T5.2, adapter=pragnosia_sft_lora.pt, base untouched) sharpened answers: 'capital of France'->'Paris.' (bleed gone). REMAINING (need scale/training, not fixable by code): knowledge gaps ('who wrote Hamlet'->'Hamlet' not Shakespeare), multi-hop (T1.8: doesn't compute intermediates), the crux matched-transformer@284M (H100), prototypes needing training (workspace-inject T2.1, self-vector T2.4, multimodal adapter). SUBMISSION: IEEE double-anonymous package done (Pragnosia_manuscript_ieee.zip + .pdf, IEEEtran journal 2-col, compiled via tectonic at ~/bin/tectonic, 7pp, author=Anonymous; title 'Language Model' no 'Small'; title page=manuscript/manuscript_titlepage.docx non-anon). tectonic installed for future LaTeX compiles. Latest model = spin_1022M_wweighted (ppl 18.56) active as pragnosia_spin.pt; identity now IN the weights (LM says 'I am Pragnosia' itself in chat format). NO em-dashes anywhere in paper/site/md docs (user requirement). **REVIEWER ROUND 3 (site de-anthropomorphized: living/alive/brain-like→adaptive/neuromodulation-inspired, LLM comparisons qualified; abstract split to short sentences; full-page arch figure Fig 1 via make_lc/arch/prog_fig.py generators).** Remaining crux (all 3 reviews): the matched 1B transformer-vs-spin run + Mamba/RWKV/RetNet baselines (H100/compute). Optional: paper-split (demote controller §4-5 to appendix) — user's positioning call. Docs (HANDOVER/RUNBOOK/TRAINING_NOTES/paper/site/ops.sh) updated + fact-checked to this state. See [[membrae-ops-and-server]] if present for server/ops details.


---

# CURRENT STATE (2026-07-17) — SUPERSEDES ANY CONFLICTING CLAIM ABOVE

**READ THIS FIRST. Several headline claims earlier in this file are now RETRACTED by measurement.**

## THE CRUX IS DECIDED: a statistical TIE (not a win)
Matched attention-only transformer @284M (289M, `carrier=none`, d768/L21/mm9, `pragnosia_baseline.json`)
vs the fair-lr spin 284M: **21.01 vs 20.92** best-val, matched corpus/tokenizer/budget (418K steps, 6.85B
tokens), both with the lr fix, both from scratch, NO growth. My 200-it harness: spin 22.32 vs tf
22.89(best)/22.22(final) — the tf's own best-vs-final spread (~0.7 ppl) EXCEEDS the spin-vs-tf gap. **TIE.**

## THE SCALE-DECAY (the honest centerpiece)
**37M ~21% → 100M ~0.9% (n.s.) → 284M tie.** Multi-seed @100M (`msweep.sh`, param-matched pair, 3 PAIRED
seeds, matched tokens/lr): spin 28.80 vs attn 29.06 = **+0.88%**, ahead 3/3 seeds (direction consistent) but
ranges overlap, **p≈0.14 not significant**, per-seed deltas shrink (0.45→0.23→0.09).
**CLAIM: spin-dominant is COMPETITIVE at scale, NOT superior. Never say "wins" unqualified — only at 37M.**

## What DOES hold: the causal result (the paper's centerpiece)
Ablating the carrier costs **~2 orders of magnitude** more than ablating attention — at every scale and in
**all 3 seeds** (`multiseed_ablate.py`, n=3 @100M: gaps 66x/108x/74x). On the CORRECTED fair-lr 284M:
313x vs 4.6x (gap 68x).

## RETRACTED — do NOT quote these again
- **The 306x → 767x → 1886x "grows more load-bearing" progression.** The exact multiplier is numerically
  UNSTABLE: matched-fraction ablation is non-monotone (7-spin 5611x vs all-22 1695x), and across seeds the
  carrier multiplier ranges **199–306x** while attention is stable **2.83–3.02x**. **Quote the GAP
  (order-of-magnitude), never a precise multiplier.**
- "spin-dominant wins by 21%" as a headline — true ONLY at 37M.

## The lr-floor bug (f3277c1) — explains a lot of old numbers
The adaptive lr controller could only CUT lr_scale, so early-training noise ratcheted it to the floor and
PINNED it. This **crippled the crux baseline (stuck at ppl 174.8!)** and floored the 1B at 7.5e-5. Fixed by a
warm-restart branch (auto-RAISE x4 on a floored sustained plateau) + `WARM` env. Also fixed: the grow
writeback clobbered a HARDCODED `pragnosia.json` (observed d768 -> d384!) — now writes to CONFIG_PATH; and
`SEED` env added (the seed was hardcoded to 0, so multi-seed was impossible before).
**TWO 284M RUNS EXIST:** the original (val ppl **24.54**, ablation 306x) predates the fix and is CRIPPLED;
the fair-lr re-run (**20.92**, `pragnosia_284m_fair.pt`) is the crux model. The bug moved the perplexity, NOT
the conclusion. The paper explains both explicitly.

## Coupling (fast-slow / DMP-inspired) = an honest NEGATIVE. Do not revive without new evidence.
`CoupledBlock` + `carrier="spin_dominant_coupled"` (compact slow state modulates the fast attention pathway,
after the NMI DMP paper). From-scratch 284M vs an identical control differing ONLY in the coupling (100K
steps, matched lr): **coupled is 7.2% WORSE** (24.87 -> 26.65). Second independent negative (frozen-1B probe
was neutral). **Stage B (scale to 1B) CANCELLED per the pre-registered criterion. Keep the parallel design.**
LESSON: warm-start-continue on a CONVERGED base cannot test added capacity (no headroom — both arms only
drift up); such tests must be FROM SCRATCH.

## PAPER: target = Nature Machine Intelligence. IEEE IS DROPPED.
- `manuscript_nature/` (two-column, 13pp, `pragnosia_nature.pdf`) = the readable/preprint copy.
- `manuscript_blinded/` (`pragnosia_blinded_manuscript.pdf`) = the SUBMISSION blinded manuscript (no name,
  no email, no repo URL — re-verify blinding after any edit).
- `pragnosia_title_page.pdf` + `title_page/` = the separate Title Page. **GITIGNORED ON PURPOSE — it carries
  the author's postal address. Never commit it.**
- Abstract trimmed 382 -> 202 words (Nature length). Theory = "Properties of the carrier": Props 1–2
  (stability, memory horizon) are cited to LRU/S4D as **inherited background, NOT ours**; **Prop 3**
  (phase decouples retention from lag-indexing; an isometry) is ours, framed as a FALSIFIABLE prediction
  (the phase advantage should grow with context length) — consistent with the measured "placement, not
  phase, at the tested budget".
- Positioning: cite the NMI review (`tiezzi2025recurrent`) as the question we answer causally, and the DMP
  fast-slow paper (`sun2026dualmemory`). The controller is demoted to a **"preliminary companion, not the
  central claim"**; the centerpiece is (i) the architecture + (ii) the causal evidence.
- `RESULTS_MEASURED.md` = the single source of truth for every number (19 sections).

## Other measured state
Router = precision-first derived margin (held-out: 83% negative specificity, 28% recall) — a KNOWN-WEAK
component, reported honestly (the old "14/14" was overfit to its own eval). Monologue = honesty-gated
consolidation (`_worth_consolidating`: novel AND passes the truth probe) — blocks 65% of self-generated
thoughts as confabulation, preventing self-poisoning; the monologue is currently SAFE but INERT (novelty
gate blocks all consolidation) and LM-capped. Needle: retrieval 0.47 (L128) -> 0.07 (L2048) — dies past the
256 train window. MMLU 27.9% (chance 25), GSM8K 1.3%. Activation probe separates known 0.83 vs nonsense 0.00.

## In flight / next
H100 training the **512M rung -> 1B** with a mixed-window schedule (30% W1 / 30% W32 / 40% middle) + the lr
fix. When it lands: run `eval_battery.py` + `needle_eval.py` and fold W1/W8/W32 + the needle curve into the
paper's long-context section — that is the LAST measured gap. Optional/low value: GPQA, RULER, a deeper
Prop 3. **Every rigor criticism the reviewer raised is otherwise closed** (crux, multi-seed comparison,
multi-seed ablation, coupling, MMLU/GSM8K, needle, code release, refocus, internal coherence).

## Standing rules (unchanged)
No hardcoding. No em-dashes in paper/site/docs. H100 (`ssh -i /opt/keys/prodh_key.pem
azureuser@172.191.51.191`, code at /opt/code/membrae) is READ-ONLY — pull results, never change it. Never
kill the user's Unreal Editor. Commits end with the Co-Authored-By line. Site (`site/index.html`) is now
honesty-matched to the paper (crux tie on the front page, gap not multiplier, "Why build it this way?"
plain-words explainer).
